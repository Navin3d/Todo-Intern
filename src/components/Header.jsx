import React from "react";

const Header = () => (
    <div>
        <h1 className="heading">This is the react based Todo Application</h1>
        <h4 className="sub-heading">“ Life is what happens when you're busy making other plans.” — John Lennon.</h4>
    </div>
);

export default Header;